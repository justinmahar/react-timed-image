/**
 * Simple counter hook used as an example. Returns the count and functions to increment and reset.
 */
export declare const useExampleCounter: (initialValue?: number) => {
    count: number;
    increment: () => void;
    reset: () => void;
};
